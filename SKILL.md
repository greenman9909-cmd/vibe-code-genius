# Vibe Code Genius

Load this skill once. Start at node 01, follow prerequisites in `skill-tree.json`, persist every artifact, and stop at the requested tier. Run validators after each write. Use `debug on` only when diagnosing a failure.
