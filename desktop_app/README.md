# GodTree Desktop

Native application layer.

Target stack:
- Tauri shell
- React + TypeScript UI
- Local GodTree daemon
- Windows installer

This replaces the prototype CLI as the user-facing product.
