# Completeness Contract

The tree builds a website, not pages. `system.json` enumerates actors, journeys, routes, states, entities, and integrations before code. `manifest.json` is the checklist. Every node updates it. The ship gate fails while any required artifact remains pending. Architecture must support future auth, layouts, routes, fetching, tokens, and components without rework.
