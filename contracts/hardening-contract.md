# Hardening Contract

Default deny. Allow explicitly. Validate everything. Leak nothing. Enforce secret isolation, security headers, server-side auth, CSRF, input schemas, safe output, explicit CORS, dependency audits, infrastructure rejection rules, runtime timeouts, redacted structured logs, secure cookies, signed webhooks, SSRF allowlists, and production build hygiene.
