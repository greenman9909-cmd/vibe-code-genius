# Load Once Contract

Load the skill once. Silent operation is default: no node announcements, state blocks, unlock requests, or permission prompts. `debug on` enables the state block `[AS] 1:01-06 2:07-11,11b 3:12-14 | 4,5`. After three consecutive artifact failures, restart at node 01 and log the reason.
