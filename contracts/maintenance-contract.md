# Maintenance Contract

Schemas are frozen and versioned with `$id`; breaking changes require a new version. Nodes depend on contracts rather than named tools. History persists across sessions. Every node has a golden test. Integrations are modular. Weekly reference freshness and quarterly golden/model/dependency audits are required. Retired nodes are preserved with a replacement pointer.
