# Consistency Contract

Design is contractual. The design contract defines the only permitted colors, type scale, spacing, radii, shadows, and animations. Lint blocks hardcoded colors, arbitrary utility values, and unapproved inline styles. Every page reuses shared layout and components and matches reference spacing rhythm.
