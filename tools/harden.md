# harden

Repository or runtime: `security hardener`.

Detects the application stack and patches headers, cookies, CSP, CORS, rate limits, secret exposure, and production settings. Writes harden_report.json.

## Contract

Input and output are validated before the next node consumes them. Keep the tool replaceable: alternatives must satisfy the same artifact schema.

## Support

Editable support placeholder: https://ko-fi.com/YOUR_HANDLE
