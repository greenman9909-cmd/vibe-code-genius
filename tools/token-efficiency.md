# Token efficiency

Repository or runtime: `seven-lever reference`.

Semantic density, prompt caching, context hygiene, output verbosity control, model routing, structured inter-node output, and symbolic encoding. Every token must change the result.

## Contract

Input and output are validated before the next node consumes them. Keep the tool replaceable: alternatives must satisfy the same artifact schema.

## Support

Editable support placeholder: https://ko-fi.com/YOUR_HANDLE
