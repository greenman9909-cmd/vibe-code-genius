# api-researcher

Repository or runtime: `github.com/greenman9909-cmd/api-researcher`.

Safe API behavior profiler. Runs `apiresearch --endpoints ./endpoints.txt --out ./api_research --auth-header "$TOKEN"`. Produces api_research.json and defaults to GET-only probing.

## Contract

Input and output are validated before the next node consumes them. Keep the tool replaceable: alternatives must satisfy the same artifact schema.

## Support

Editable support placeholder: https://ko-fi.com/YOUR_HANDLE
