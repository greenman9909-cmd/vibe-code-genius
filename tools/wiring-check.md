# wiring-check

Repository or runtime: `github.com/greenman9909-cmd/wiring-check`.

Reference resolver invoked by static, runtime, and build verification. Exits non-zero on unresolved imports, routes, API calls, tokens, environment variables, i18n keys, or components.

## Contract

Input and output are validated before the next node consumes them. Keep the tool replaceable: alternatives must satisfy the same artifact schema.

## Support

Editable support placeholder: https://ko-fi.com/YOUR_HANDLE
