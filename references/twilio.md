# Twilio integration notes

Document the verified auth method, base URL, rate limits, key endpoints, webhook signature verification, SDK choice, and gotchas here. Use vendor documentation as the source of truth; do not hallucinate APIs.
