# Reference

Canonical API patterns: cursor, offset, and page pagination; structured errors; path/header/query versioning; rate-limit headers; idempotency keys; signed webhooks; explicit auth headers.
